# 访问后台面板时只需要输入这个密码，请务必换成强密码。
DASHBOARD_PASSWORD=change-this-to-a-long-random-password

# 只有在面板通过 HTTPS 反向代理访问时才改为 true。
COOKIE_SECURE=false

# VPS 每个自然月的总流量额度，单位 GB（十进制，1 GB = 10^9 bytes）。
MONTHLY_QUOTA_GB=3000

# 通常无需修改。
COLLECT_INTERVAL=10
TAILSCALE_INTERFACE=tailscale0
TZ=America/Los_Angeles
